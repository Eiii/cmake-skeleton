Functional CMake project skeleton with testing

Build instructions:
  - `mkdir build; cd build`
  - `cmake ..`
  - `cmake --build .`

Test instructions:
  - (Build the project)
  - `ctest` OR `cmake --build . --target test`
  - ...OR just run the test directly so we can see the output? `lib/libtest`

